"""
Pengu OS Control — real Windows system interaction.

Modules:
  app_manager: application discovery, open, close, focus, is_running
  process_manager: process listing, info, safe termination
  filesystem: secure filesystem operations with path validation
  terminal: safe terminal execution with command allowlist
  system_info: system information tools
  vscode: VS Code integration
"""
