"""
Pengu UI module — serves the desktop interface.

Provides:
  - Static file serving for the HTML/CSS/JS frontend
  - Mounts on the FastAPI app

The UI connects to the backend via REST API and WebSocket.
"""
